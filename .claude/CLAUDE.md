# ClaudeTest1 Project

See `CLAUDE.md` at the repo root for full project guidance.

## Conventions
- Keep files simple and self-contained where possible
- Prefer plain HTML/CSS/JS for web demos (no build tools unless needed)
- Use `open <file>.html` to run any game in the browser
